import numpy as np
    # data = np.zeros((0, 2))
    # There are 5 sets of measurements per position so 5 files must be read in.
    # for j in range(testnum):
    #    if i is None:
    #        break
    #    data = np.concatenate((data, np.genfromtxt(i, delimiter=',')), axis=0)
    #    i = next(pos, None)
    # data = data.reshape((testnum, int(np.size(data) / 10), -1))